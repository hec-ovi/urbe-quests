{
  "name": "quests",
  "version": "0.2.11",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "vitest",
    "build": "tsc -p tsconfig.build.json && node scripts/copy-prompts.mjs",
    "watch": "npm run build && tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc --noEmit",
    "sample": "tsx creation/samples/run-local.ts"
  },
  "type": "module",
  "private": "true",
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "undici": "^8.10.1",
    "vitest": "^4.1.11"
  }
}
