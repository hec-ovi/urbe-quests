Validate the questline and commit it; nothing is committed until this call succeeds, and a successful call ends the build. It first names any role, item, act, ending or step from the plan's manifest that has not been added yet; once everything is in, it checks the whole flow (references, edges and endings, flags and completion effects, dialogue, named hours) and reports the first problem it finds. Fix that problem, by adding what is missing or by calling the add tool again with the same id to replace the piece at fault, and call this again until it succeeds. It takes no arguments.
